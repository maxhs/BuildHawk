$('#main').fadeOut(100,function(){
	$(this).html('<%= j render :partial => "punchlist_items/new" %>');
	$(this).fadeIn(100);
	var locations = <%= raw @locations.to_json %>; 
	$('#punchlist_item_location').autocomplete({
      	source: locations
    });
});