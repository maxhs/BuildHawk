$('a').removeClass('current-page');
var nav = $('<%= j render partial: "admin/top_nav" %>');
$('#navigation').html(nav);
$('#inner-wrap').html('<%= j render :partial => "index" %>');
$('#nav-admin a, #admin-link, #admin-checklists-link').addClass('current-page');
$('.project.active').removeClass('active');
mixpanel.track("Admin");
window.scrollTo(0,0);