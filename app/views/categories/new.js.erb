if ($('#<%=@category.id%>-items .new_category').length){
	$('#<%=@category.id%>-items .new_category').fadeOut(200,function(){
		$(this).remove();
		$('#new-category-<%=@category.id%>').text("Add a new category");
	});

} else {
	var newDiv = $('<%= j render partial:"categories/new" %>').css('display','none');
	$('#new-category-<%=@category.id%>').before(newDiv);
	newDiv.fadeIn(200);
	$('#new-category-<%=@category.id%>').text("Cancel");
}
