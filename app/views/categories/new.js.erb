if ($('.new_category').length){
	$('.new_category').fadeOut(200,function(){
		$(this).remove();
	});
} else {
	var newDiv = $('<%= j render partial:"categories/new" %>').css('display','none');
	$('#categories').before(newDiv);
	newDiv.fadeIn(200);
	
}
