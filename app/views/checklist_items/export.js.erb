showAlert("\"<%= j @item.abbreviated_body %>\" sent to <%= j @recipients.to_sentence %>.")
$('#export-checklist-modal').animate({"left":"100%",'opacity':'0'},300);
$('.modal-backdrop.in').fadeOut(300,function(){
	$(this).remove();
});