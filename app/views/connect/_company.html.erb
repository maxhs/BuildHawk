<h3><%= company.name %></h3>
<div id="connect-company" class="border-box">
	<% projects = @items.map{|t| t.worklist.project if t.worklist.project.company && t.worklist.project.company.id == company.id}.uniq %>
	
</div>