$('#pages').fadeOut(100,function(){
	$(this).html('<%= j render :partial => "edit_worklist_item", :locals => {:punchlist_item => @punchlist_item, :project => @project } %>');
	$(this).fadeIn(100);
});