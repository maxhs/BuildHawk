<li class="category" id="phase-<%=c.id%>">
	<% if current_user.admin %>
		<%= link_to "Edit", category_path(c, :project_id => @project), :class => "left-edit", remote: true %>
	<% end %>
	<div class="header row">
		<span class="col-xs-8"><h2 style="margin: 15px auto;display:inline-block"><%= link_to c.name, load_items_checklist_path(c, :project_id => @project.id), :id => "c-#{c.id}", :class=>"phase-name remote", :remote => true %></h2></span>
		<span class="col-xs-2 phase-label"><%= parse_date(c.milestone_date) %></span>	
		<span class="col-xs-2 phase-label"><%= parse_date(c.completed_date) %></span>
	</div>
	<ul id="<%= c.id %>-items"></ul>
</li>