if ($('#add-sub-section').length){
	$('#add-sub-section').html('<%= j render partial: "company_subs/new", locals: {subcontractor: @subcontractor } %>');
	$("html, body").animate({ scrollTop: 0 }, 0);
} else {
	$('#main').html('<%= j render partial: "company_subs/new", locals: {subcontractor: @subcontractor } %>');
	$("html, body").animate({ scrollTop: 0 }, 0);
}