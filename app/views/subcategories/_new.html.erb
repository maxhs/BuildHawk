<%= nested_form_for @subcategory, :remote => true, :method => :post do |f| %>
	<%= f.hidden_field :category_id, :value => @category.id %>
	<div class="row">
		<div class="col-xs-10"><%= f.text_field :name, :placeholder => "Name the new category...", :style=>"width:96%;margin-left:2%;margin-right:-2%;" %></div>
		<%= f.submit "Add", :class => "btn btn-success btn-md pull-right col-xs-2", :style=>"margin:0 auto;padding-top: 0;line-height: 36px;height: 36px;margin: 0 auto;" %>
	</div>
<% end %>