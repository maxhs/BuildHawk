<h1 style="text-align:center;margin:0 auto;"> <%= @project.name %> </h1>
<h2 style="text-align:center;margin:5px auto;"> Worklist Report </h2>

<% @item_array.each do |item| %>

	<div style="width:100%;clear:both;">
		<% if item.photos && item.photos.count > 0 && item.photos.first.image_file_name %>
			<% photo = item.photos.first %>
			<%= link_to edit_punchlist_item_url(item, :project_id => @project.id, :host => "www.buildhawk.com"), :style=>"float:left;margin:0 10px 10px 0;" do %>
				<%= image_tag(photo.image.url(:small), :style => "width:100px;display:block;") %> 
			<% end %>
		<% else %>
			<div style="float:left;margin:0 10px 10px 0;background:#eee;line-height:100px;height:100px;width:100px;text-align:center;vertical-align:middle;color:#aaa;">No photo</div>
		<% end %>

		<span style="font-size:16px;"><%= link_to item.body, edit_punchlist_item_url(item, :project_id => @project.id, :host => "www.buildhawk.com"), :style=>"color:#000;text-decoration:none;" %></span>

		<% if item.assignee || item.sub_assignee %>
			<div>
				<h4 style="display:inline-block;margin:10px 0;">Assigned:</h4>
				<span>
					<% if item.assignee %>
						<%= item.assignee.full_name %>
					<% elsif item.sub_assignee %>
						<%= item.sub_assignee.name %>
					<% end %>
				</span>
			</div>
		<% end %>
		<div><h4 style="display:inline-block;margin:0 auto;">Date:</h4><%= item.created_at.strftime("%b %e, %l:%M %p") %></div>
		
		<% if item.completed %>
			<div><h4 style="display:inline-block;margin:0 auto;">Status:</h4><span style="padding-left:2em;font-weight:normal;">Complete</span></div>
		<% else %>
			<div><h4 style="display:inline-block;margin:0 auto;">Status:</h4><span style="padding-left:2em;color:#cc0000;font-weight:normal;">IN PROGRESS</span></div>
		<% end %>
	</div>

<% end %>
