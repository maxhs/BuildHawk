<% if @admin %>
	showAlert('User created');
	$('#navigation a').removeClass('current-page');
	$('#personnel-link').addClass('current-page');
	$('#main').html('<%= j render partial: "admin/personnel", locals: {users: @users } %>');
	$("html, body").animate({ scrollTop: 0 }, 0);
<% else %>

<% end %>